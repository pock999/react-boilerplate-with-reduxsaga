import NegativeConfirmAlert from './NegativeConfirmAlert';
import PositiveConfirmAlert from './PositiveConfirmAlert';

// 這2個組件為有確認以及取消等動作，但是並無計時器的功能，一樣可以放置訊息

const ConfirmAlert = {
  NegativeConfirmAlert,
  PositiveConfirmAlert,
};

export default ConfirmAlert;
